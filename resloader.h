// created to isolate windows api calls from raylib due to conflicts

// function to load packaged resources
unsigned char* LoadPackagedResource(unsigned long typeId, unsigned long resourceId, unsigned long* size);
